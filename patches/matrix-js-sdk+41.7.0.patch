diff --git a/node_modules/matrix-js-sdk/lib/client.js b/node_modules/matrix-js-sdk/lib/client.js
index bc4e623..5e31c68 100644
--- a/node_modules/matrix-js-sdk/lib/client.js
+++ b/node_modules/matrix-js-sdk/lib/client.js
@@ -1882,13 +1882,38 @@ export class MatrixClient extends TypedEventEmitter {
     // that hasn't been sent yet (e.g. with a local id starting with a ~)
     // then listen for the remote echo of that event so that by the time
     // this event does get sent, we have the correct event_id
-    var targetId = localEvent.getAssociatedId();
-    if (targetId !== null && targetId !== void 0 && targetId.startsWith("~")) {
-      var target = room === null || room === void 0 ? void 0 : room.getPendingEvents().find(e => e.getId() === targetId);
+    var localEventIdPrefix = room ? "~".concat(room.roomId, ":") : undefined;
+    var targetIds = new Set([localEvent.relationEventId, localEvent.replyEventId, localEvent.isRedaction() ? localEvent.event.redacts : undefined]);
+    var pendingThreadRoot;
+    targetIds.forEach(targetId => {
+      if (!targetId || !localEventIdPrefix || !targetId.startsWith(localEventIdPrefix)) return;
+      var txnId = targetId.slice(localEventIdPrefix.length);
+      var _room$getEventForTxn, _room$findEventById;
+      var target = (_room$getEventForTxn = room === null || room === void 0 ? void 0 : room.getEventForTxnId(txnId)) !== null && _room$getEventForTxn !== void 0 ? _room$getEventForTxn : (_room$findEventById = room === null || room === void 0 ? void 0 : room.findEventById(targetId)) !== null && _room$findEventById !== void 0 ? _room$findEventById : room === null || room === void 0 ? void 0 : room.getPendingEvent(targetId);
+      if (!target) {
+        var loadedEvents = room ? [room.getUnfilteredTimelineSet(), ...room.getThreads().map(thread => thread.getUnfilteredTimelineSet())].flatMap(timelineSet => timelineSet.getTimelines()).flatMap(timeline => timeline.getEvents()) : [];
+        target = loadedEvents.find(event => {
+          var _event$getTxnId;
+          var eventTxnId = (_event$getTxnId = event.getTxnId()) !== null && _event$getTxnId !== void 0 ? _event$getTxnId : event.getUnsigned().transaction_id;
+          return eventTxnId === txnId;
+        });
+      }
+      var replacesThreadRoot = localEvent.isRelation(THREAD_RELATION_TYPE.name) && localEvent.relationEventId === targetId;
+      if (replacesThreadRoot) {
+        pendingThreadRoot = target;
+      }
+      var resolvedTargetId = target === null || target === void 0 ? void 0 : target.getId();
+      if (resolvedTargetId && resolvedTargetId !== targetId) {
+        localEvent.updateAssociatedId(resolvedTargetId, targetId);
+        return;
+      }
       target === null || target === void 0 || target.once(MatrixEventEvent.LocalEventIdReplaced, () => {
-        localEvent.updateAssociatedId(target.getId());
+        localEvent.updateAssociatedId(target.getId(), targetId);
+        if (replacesThreadRoot) {
+          room === null || room === void 0 || room.updatePendingEventRelationTarget(localEvent, targetId, target);
+        }
       });
-    }
+    });
     var type = localEvent.getType();
     this.logger.debug("sendEvent of type ".concat(type, " in ").concat(roomId, " with txnId ").concat(txnId).concat(delayOpts ? " (delayed event)" : "").concat(queryDict ? " query params: " + JSON.stringify(queryDict) : ""));
     localEvent.setTxnId(txnId);
@@ -1898,6 +1923,14 @@ export class MatrixClient extends TypedEventEmitter {
     if (!delayOpts) {
       // add this event immediately to the local store as 'sending'.
       room === null || room === void 0 || room.addPendingEvent(localEvent, txnId);
+      if (pendingThreadRoot && localEvent.isRelation(THREAD_RELATION_TYPE.name) && localEvent.threadRootId === pendingThreadRoot.getId()) {
+        var _room$getThread;
+        var threadRootId = pendingThreadRoot.getId();
+        var pendingThread = (_room$getThread = room === null || room === void 0 ? void 0 : room.getThread(threadRootId)) !== null && _room$getThread !== void 0 ? _room$getThread : room === null || room === void 0 ? void 0 : room.createThread(threadRootId, pendingThreadRoot, [], false);
+        if (pendingThread && !pendingThread.findEventById(localEvent.getId())) {
+          pendingThread.addEvent(localEvent, false);
+        }
+      }

       // addPendingEvent can change the state to NOT_SENT if it believes
       // that there's other events that have failed. We won't bother to
diff --git a/node_modules/matrix-js-sdk/lib/models/event.d.ts b/node_modules/matrix-js-sdk/lib/models/event.d.ts
index ef37343..3c48643 100644
--- a/node_modules/matrix-js-sdk/lib/models/event.d.ts
+++ b/node_modules/matrix-js-sdk/lib/models/event.d.ts
@@ -761,8 +761,9 @@ export declare class MatrixEvent extends TypedEventEmitter<MatrixEventEmittedEve
      * an event with a related id.
      *
      * @param eventId - the new event id
+     * @param originalEventId - the original local event id to replace
      */
-    updateAssociatedId(eventId: string): void;
+    updateAssociatedId(eventId: string, originalEventId?: string): void;
     /**
      * Flags an event as cancelled due to future conditions. For example, a verification
      * request event in the same sync transaction may be flagged as cancelled to warn
diff --git a/node_modules/matrix-js-sdk/lib/models/event.js b/node_modules/matrix-js-sdk/lib/models/event.js
index 41c0e07..32240c2 100644
--- a/node_modules/matrix-js-sdk/lib/models/event.js
+++ b/node_modules/matrix-js-sdk/lib/models/event.js
@@ -1417,8 +1417,23 @@ export class MatrixEvent extends TypedEventEmitter {
    * an event with a related id.
    *
    * @param eventId - the new event id
-   */
-  updateAssociatedId(eventId) {
+   * @param originalEventId - the original local event id to replace
+   */
+  updateAssociatedId(eventId, originalEventId) {
+    if (originalEventId !== undefined) {
+      var _relatesTo$mIn_reply;
+      var relatesTo = this.getWireContent()["m.relates_to"];
+      if ((relatesTo === null || relatesTo === void 0 ? void 0 : relatesTo.event_id) === originalEventId) {
+        relatesTo.event_id = eventId;
+      }
+      if ((relatesTo === null || relatesTo === void 0 || (_relatesTo$mIn_reply = relatesTo["m.in_reply_to"]) === null || _relatesTo$mIn_reply === void 0 ? void 0 : _relatesTo$mIn_reply.event_id) === originalEventId) {
+        relatesTo["m.in_reply_to"].event_id = eventId;
+      }
+      if (this.isRedaction() && this.event.redacts === originalEventId) {
+        this.event.redacts = eventId;
+      }
+      return;
+    }
     var relation = this.getRelation();
     if (relation) {
       relation.event_id = eventId;
diff --git a/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts b/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts
index ba649eb..46c53af 100644
--- a/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts
+++ b/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts
@@ -25,6 +25,15 @@ export declare class RelationsContainer {
      */
     getChildEventsForEvent(eventId: string, relationType: RelationType | string, eventType: EventType | string): Relations | undefined;
     getAllChildEventsForEvent(parentEventId: string): MatrixEvent[];
+    /**
+     * Remove a child event and prune its relation collection when it becomes empty.
+     *
+     * @param eventId - The relation target event ID.
+     * @param relationType - The relation type.
+     * @param eventType - The child event type.
+     * @param event - The child event to remove.
+     */
+    removeChildEvent(eventId: string, relationType: RelationType | string, eventType: EventType | string, event: MatrixEvent): void;
     /**
      * Set an event as the target event if any Relations exist for it already.
      * Child events can point to other child events as their parent, so this method may be
diff --git a/node_modules/matrix-js-sdk/lib/models/relations-container.js b/node_modules/matrix-js-sdk/lib/models/relations-container.js
index 8ccc053..12d2b9d 100644
--- a/node_modules/matrix-js-sdk/lib/models/relations-container.js
+++ b/node_modules/matrix-js-sdk/lib/models/relations-container.js
@@ -56,6 +56,31 @@ export class RelationsContainer {
     return events;
   }

+  /**
+   * Remove a child event and prune its relation collection when it becomes empty.
+   *
+   * @param eventId - The relation target event ID.
+   * @param relationType - The relation type.
+   * @param eventType - The child event type.
+  * @param event - The child event to remove.
+  */
+  removeChildEvent(eventId, relationType, eventType, event) {
+    var relationsForEvent = this.relations.get(eventId);
+    if (!relationsForEvent) return;
+    var relationsWithRelType = relationsForEvent.get(relationType);
+    if (!relationsWithRelType) return;
+    var relations = relationsWithRelType.get(eventType);
+    if (!relations) return;
+    void relations.removeEvent(event);
+    if (relations.getRelations().length > 0) return;
+    relationsWithRelType.delete(eventType);
+    if (relationsWithRelType.size > 0) return;
+    relationsForEvent.delete(relationType);
+    if (relationsForEvent.size === 0) {
+      this.relations.delete(eventId);
+    }
+  }
+
   /**
    * Set an event as the target event if any Relations exist for it already.
    * Child events can point to other child events as their parent, so this method may be
diff --git a/node_modules/matrix-js-sdk/lib/models/relations.js b/node_modules/matrix-js-sdk/lib/models/relations.js
index 6be66bb..6f70de0 100644
--- a/node_modules/matrix-js-sdk/lib/models/relations.js
+++ b/node_modules/matrix-js-sdk/lib/models/relations.js
@@ -167,6 +167,9 @@ export class Relations extends TypedEventEmitter {
         return;
       }
       _this3.relations.delete(event);
+      _this3.relationEventIds.delete(event.getId());
+      event.removeListener(MatrixEventEvent.Status, _this3.onEventStatus);
+      event.removeListener(MatrixEventEvent.BeforeRedaction, _this3.onBeforeRedaction);
       if (_this3.relationType === RelationType.Annotation) {
         _this3.removeAnnotationFromAggregation(event);
       } else if (_this3.relationType === RelationType.Replace) {
diff --git a/node_modules/matrix-js-sdk/lib/models/room.d.ts b/node_modules/matrix-js-sdk/lib/models/room.d.ts
index 83c2a44..38372a5 100644
--- a/node_modules/matrix-js-sdk/lib/models/room.d.ts
+++ b/node_modules/matrix-js-sdk/lib/models/room.d.ts
@@ -902,6 +902,13 @@ export declare class Room extends ReadReceipt<RoomEmittedEvents, RoomEventHandle
     private updateThreadRootEvents;
     private updateThreadRootEvent;
     createThread(threadId: string, rootEvent: MatrixEvent | undefined, events: MatrixEvent[] | undefined, toStartOfTimeline: boolean): Thread;
+    /**
+     * Reaggregate a pending relation after its local thread-root id is replaced
+     * and move any provisional thread events to the canonical thread.
+     *
+     * @internal
+     */
+    updatePendingEventRelationTarget(event: MatrixEvent, oldTargetId: string, targetEvent: MatrixEvent): void;
     /**
      * Applies an event as a redaction of another event, regardless of whether the redacting
      * event is actually a redaction.
diff --git a/node_modules/matrix-js-sdk/lib/models/room.js b/node_modules/matrix-js-sdk/lib/models/room.js
index ac723cf..e8d25bb 100644
--- a/node_modules/matrix-js-sdk/lib/models/room.js
+++ b/node_modules/matrix-js-sdk/lib/models/room.js
@@ -1223,10 +1223,10 @@ export class Room extends ReadReceipt {
     var event = this.findEventById(eventId);
     var thread = this.findThreadForEvent(event);
     if (thread) {
-      return thread.timelineSet.getTimelineForEvent(eventId);
-    } else {
-      return this.getUnfilteredTimelineSet().getTimelineForEvent(eventId);
+      var timeline = thread.timelineSet.getTimelineForEvent(eventId);
+      if (timeline) return timeline;
     }
+    return this.getUnfilteredTimelineSet().getTimelineForEvent(eventId);
   }

   /**
@@ -2241,6 +2241,45 @@ export class Room extends ReadReceipt {
     return thread;
   }

+  /**
+   * Reaggregate a pending relation after its local thread-root id is replaced
+   * and move any provisional thread events to the canonical thread.
+   *
+   * @internal
+   */
+  updatePendingEventRelationTarget(event, oldTargetId, targetEvent) {
+    var relation = event.getRelation();
+    var targetEventId = targetEvent.getId();
+    if (!(relation !== null && relation !== void 0 && relation.rel_type) || !targetEventId || relation.event_id !== targetEventId) return;
+    this.relations.removeChildEvent(oldTargetId, relation.rel_type, event.getType(), event);
+    this.relations.aggregateChildEvent(event);
+    if (!event.isRelation(THREAD_RELATION_TYPE.name) || oldTargetId === targetEventId) return;
+    var provisionalThread = this.getThread(oldTargetId);
+    var threadEvents = [...(provisionalThread ? provisionalThread.events : [])];
+    if (!threadEvents.includes(event)) {
+      threadEvents.push(event);
+    }
+    if (provisionalThread) {
+      this.threads.delete(oldTargetId);
+      this.reEmitter.stopReEmitting(provisionalThread, [ThreadEvent.Delete, ThreadEvent.Update, ThreadEvent.NewReply, RoomEvent.Timeline, RoomEvent.TimelineReset]);
+      provisionalThread.dispose();
+      for (var timelineSet of this.threadsTimelineSets) {
+        timelineSet.removeEvent(oldTargetId);
+      }
+    }
+    var canonicalThread = this.getThread(targetEventId) || this.createThread(targetEventId, targetEvent, [], false);
+    for (var threadEvent of threadEvents) {
+      var _canonicalThread$repl;
+      if (!canonicalThread.initialEventsFetched && !((_canonicalThread$repl = canonicalThread.replayEvents) !== null && _canonicalThread$repl !== void 0 && _canonicalThread$repl.includes(threadEvent))) {
+        var _canonicalThread$repl2;
+        (_canonicalThread$repl2 = canonicalThread.replayEvents) === null || _canonicalThread$repl2 === void 0 || _canonicalThread$repl2.push(threadEvent);
+      }
+      if (!canonicalThread.findEventById(threadEvent.getId())) {
+        canonicalThread.addEvent(threadEvent, false);
+      }
+    }
+  }
+
   /**
    * Applies an event as a redaction of another event, regardless of whether the redacting
    * event is actually a redaction.
diff --git a/node_modules/matrix-js-sdk/lib/models/thread.d.ts b/node_modules/matrix-js-sdk/lib/models/thread.d.ts
index 6933b26..8503ea3 100644
--- a/node_modules/matrix-js-sdk/lib/models/thread.d.ts
+++ b/node_modules/matrix-js-sdk/lib/models/thread.d.ts
@@ -98,6 +98,12 @@ export declare class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEvent
      */
     replayEvents: MatrixEvent[] | null;
     constructor(id: string, rootEvent: MatrixEvent | undefined, opts: IThreadOpts);
+    /**
+     * Detach this thread from its room and timeline sources.
+     *
+     * @internal
+     */
+    dispose(): void;
     private onTimelineReset;
     private fetchRootEvent;
     static setServerSideSupport(status: FeatureSupport): void;
diff --git a/node_modules/matrix-js-sdk/lib/models/thread.js b/node_modules/matrix-js-sdk/lib/models/thread.js
index b02b81f..8781cb0 100644
--- a/node_modules/matrix-js-sdk/lib/models/thread.js
+++ b/node_modules/matrix-js-sdk/lib/models/thread.js
@@ -216,6 +216,19 @@ export class Thread extends ReadReceipt {
     this.updateThreadMetadata();
     this.setEventMetadata(this.rootEvent);
   }
+  /**
+   * Detach this thread from its room and timeline sources.
+   *
+   * @internal
+   */
+  dispose() {
+    this.reEmitter.stopReEmitting(this.timelineSet, [RoomEvent.Timeline, RoomEvent.TimelineReset]);
+    this.room.off(MatrixEventEvent.BeforeRedaction, this.onBeforeRedaction);
+    this.room.off(RoomEvent.Redaction, this.onRedaction);
+    this.room.off(RoomEvent.LocalEchoUpdated, this.onLocalEcho);
+    this.room.off(RoomEvent.TimelineReset, this.onTimelineReset);
+    this.timelineSet.off(RoomEvent.Timeline, this.onTimelineEvent);
+  }
   fetchRootEvent() {
     var _this2 = this;
     return _asyncToGenerator(function* () {
@@ -549,6 +562,10 @@ export class Thread extends ReadReceipt {
     var _this7 = this;
     return _asyncToGenerator(function* () {
       _this7.updatePendingReplyCount();
+      if (_this7.id.startsWith("~".concat(_this7.roomId, ":"))) {
+        _this7.emit(ThreadEvent.Update, _this7);
+        return;
+      }
       if (!_this7.processRootEventPromise) {
         // We only want to do this once otherwise we end up rolling back to the last unsigned summary we have for the thread
         _this7.processRootEventPromise = _this7.updateThreadFromRootEvent();
diff --git a/node_modules/matrix-js-sdk/src/client.ts b/node_modules/matrix-js-sdk/src/client.ts
index 80ea382..caa022b 100644
--- a/node_modules/matrix-js-sdk/src/client.ts
+++ b/node_modules/matrix-js-sdk/src/client.ts
@@ -2853,11 +2853,45 @@ export class MatrixClient extends TypedEventEmitter<EmittedEvents, ClientEventHa
         // that hasn't been sent yet (e.g. with a local id starting with a ~)
         // then listen for the remote echo of that event so that by the time
         // this event does get sent, we have the correct event_id
-        const targetId = localEvent.getAssociatedId();
-        if (targetId?.startsWith("~")) {
-            const target = room?.getPendingEvents().find((e) => e.getId() === targetId);
+        const localEventIdPrefix = room ? `~${room.roomId}:` : undefined;
+        const targetIds = new Set([
+            localEvent.relationEventId,
+            localEvent.replyEventId,
+            localEvent.isRedaction() ? localEvent.event.redacts : undefined,
+        ]);
+        let pendingThreadRoot: MatrixEvent | undefined;
+        for (const targetId of targetIds) {
+            if (!targetId || !localEventIdPrefix || !targetId.startsWith(localEventIdPrefix)) continue;
+
+            const txnId = targetId.slice(localEventIdPrefix.length);
+            let target =
+                room?.getEventForTxnId(txnId) ?? room?.findEventById(targetId) ?? room?.getPendingEvent(targetId);
+            if (!target) {
+                const loadedEvents = room
+                    ? [room.getUnfilteredTimelineSet(), ...room.getThreads().map((thread) => thread.getUnfilteredTimelineSet())]
+                          .flatMap((timelineSet) => timelineSet.getTimelines())
+                          .flatMap((timeline) => timeline.getEvents())
+                    : [];
+                target = loadedEvents.find((event) => {
+                    const eventTxnId = event.getTxnId() ?? event.getUnsigned().transaction_id;
+                    return eventTxnId === txnId;
+                });
+            }
+            const replacesThreadRoot =
+                localEvent.isRelation(THREAD_RELATION_TYPE.name) && localEvent.relationEventId === targetId;
+            if (replacesThreadRoot) {
+                pendingThreadRoot = target;
+            }
+            const resolvedTargetId = target?.getId();
+            if (resolvedTargetId && resolvedTargetId !== targetId) {
+                localEvent.updateAssociatedId(resolvedTargetId, targetId);
+                continue;
+            }
             target?.once(MatrixEventEvent.LocalEventIdReplaced, () => {
-                localEvent.updateAssociatedId(target.getId()!);
+                localEvent.updateAssociatedId(target.getId()!, targetId);
+                if (replacesThreadRoot) {
+                    room?.updatePendingEventRelationTarget(localEvent, targetId, target);
+                }
             });
         }

@@ -2873,6 +2907,18 @@ export class MatrixClient extends TypedEventEmitter<EmittedEvents, ClientEventHa
         if (!delayOpts) {
             // add this event immediately to the local store as 'sending'.
             room?.addPendingEvent(localEvent, txnId);
+            if (
+                pendingThreadRoot &&
+                localEvent.isRelation(THREAD_RELATION_TYPE.name) &&
+                localEvent.threadRootId === pendingThreadRoot.getId()
+            ) {
+                const threadRootId = pendingThreadRoot.getId()!;
+                const pendingThread =
+                    room?.getThread(threadRootId) ?? room?.createThread(threadRootId, pendingThreadRoot, [], false);
+                if (pendingThread && !pendingThread.findEventById(localEvent.getId()!)) {
+                    pendingThread.addEvent(localEvent, false);
+                }
+            }

             // addPendingEvent can change the state to NOT_SENT if it believes
             // that there's other events that have failed. We won't bother to
diff --git a/node_modules/matrix-js-sdk/src/models/event.ts b/node_modules/matrix-js-sdk/src/models/event.ts
index 19a2f43..e3a0461 100644
--- a/node_modules/matrix-js-sdk/src/models/event.ts
+++ b/node_modules/matrix-js-sdk/src/models/event.ts
@@ -1623,8 +1623,23 @@ export class MatrixEvent extends TypedEventEmitter<MatrixEventEmittedEvents, Mat
      * an event with a related id.
      *
      * @param eventId - the new event id
+     * @param originalEventId - the original local event id to replace
      */
-    public updateAssociatedId(eventId: string): void {
+    public updateAssociatedId(eventId: string, originalEventId?: string): void {
+        if (originalEventId !== undefined) {
+            const relatesTo = this.getWireContent()["m.relates_to"];
+            if (relatesTo?.event_id === originalEventId) {
+                relatesTo.event_id = eventId;
+            }
+            if (relatesTo?.["m.in_reply_to"]?.event_id === originalEventId) {
+                relatesTo["m.in_reply_to"].event_id = eventId;
+            }
+            if (this.isRedaction() && this.event.redacts === originalEventId) {
+                this.event.redacts = eventId;
+            }
+            return;
+        }
+
         const relation = this.getRelation();
         if (relation) {
             relation.event_id = eventId;
diff --git a/node_modules/matrix-js-sdk/src/models/relations-container.ts b/node_modules/matrix-js-sdk/src/models/relations-container.ts
index 8394bc6..382a0bf 100644
--- a/node_modules/matrix-js-sdk/src/models/relations-container.ts
+++ b/node_modules/matrix-js-sdk/src/models/relations-container.ts
@@ -65,6 +65,39 @@ export class RelationsContainer {
         return events;
     }

+    /**
+     * Remove a child event and prune its relation collection when it becomes empty.
+     *
+     * @param eventId - The relation target event ID.
+     * @param relationType - The relation type.
+     * @param eventType - The child event type.
+     * @param event - The child event to remove.
+     */
+    public removeChildEvent(
+        eventId: string,
+        relationType: RelationType | string,
+        eventType: EventType | string,
+        event: MatrixEvent,
+    ): void {
+        const relationsForEvent = this.relations.get(eventId);
+        if (!relationsForEvent) return;
+        const relationsWithRelType = relationsForEvent.get(relationType);
+        if (!relationsWithRelType) return;
+        const relations = relationsWithRelType.get(eventType);
+        if (!relations) return;
+
+        void relations.removeEvent(event);
+        if (relations.getRelations().length > 0) return;
+
+        relationsWithRelType.delete(eventType);
+        if (relationsWithRelType.size > 0) return;
+
+        relationsForEvent.delete(relationType);
+        if (relationsForEvent.size === 0) {
+            this.relations.delete(eventId);
+        }
+    }
+
     /**
      * Set an event as the target event if any Relations exist for it already.
      * Child events can point to other child events as their parent, so this method may be
diff --git a/node_modules/matrix-js-sdk/src/models/relations.ts b/node_modules/matrix-js-sdk/src/models/relations.ts
index 77ba3b3..0f8c053 100644
--- a/node_modules/matrix-js-sdk/src/models/relations.ts
+++ b/node_modules/matrix-js-sdk/src/models/relations.ts
@@ -129,6 +129,9 @@ export class Relations extends TypedEventEmitter<RelationsEvent, EventHandlerMap
         }

         this.relations.delete(event);
+        this.relationEventIds.delete(event.getId()!);
+        event.removeListener(MatrixEventEvent.Status, this.onEventStatus);
+        event.removeListener(MatrixEventEvent.BeforeRedaction, this.onBeforeRedaction);

         if (this.relationType === RelationType.Annotation) {
             this.removeAnnotationFromAggregation(event);
diff --git a/node_modules/matrix-js-sdk/src/models/room.ts b/node_modules/matrix-js-sdk/src/models/room.ts
index f288f9d..a26fdc9 100644
--- a/node_modules/matrix-js-sdk/src/models/room.ts
+++ b/node_modules/matrix-js-sdk/src/models/room.ts
@@ -1465,10 +1465,10 @@ export class Room extends ReadReceipt<RoomEmittedEvents, RoomEventHandlerMap> {
         const event = this.findEventById(eventId);
         const thread = this.findThreadForEvent(event);
         if (thread) {
-            return thread.timelineSet.getTimelineForEvent(eventId);
-        } else {
-            return this.getUnfilteredTimelineSet().getTimelineForEvent(eventId);
+            const timeline = thread.timelineSet.getTimelineForEvent(eventId);
+            if (timeline) return timeline;
         }
+        return this.getUnfilteredTimelineSet().getTimelineForEvent(eventId);
     }

     /**
@@ -2577,6 +2577,66 @@ export class Room extends ReadReceipt<RoomEmittedEvents, RoomEventHandlerMap> {
         return thread;
     }

+    /**
+     * Reaggregate a pending relation after its local thread-root id is replaced
+     * and move any provisional thread events to the canonical thread.
+     *
+     * @internal
+     */
+    public updatePendingEventRelationTarget(
+        event: MatrixEvent,
+        oldTargetId: string,
+        targetEvent: MatrixEvent,
+    ): void {
+        const relation = event.getRelation();
+        const targetEventId = targetEvent.getId();
+        if (!relation?.rel_type || !targetEventId || relation.event_id !== targetEventId) return;
+
+        this.relations.removeChildEvent(
+            oldTargetId,
+            relation.rel_type,
+            event.getType(),
+            event,
+        );
+        this.relations.aggregateChildEvent(event);
+
+        if (!event.isRelation(THREAD_RELATION_TYPE.name) || oldTargetId === targetEventId) return;
+
+        const provisionalThread = this.getThread(oldTargetId);
+        const threadEvents = [...(provisionalThread?.events ?? [])];
+        if (!threadEvents.includes(event)) {
+            threadEvents.push(event);
+        }
+        if (provisionalThread) {
+            this.threads.delete(oldTargetId);
+            this.reEmitter.stopReEmitting(provisionalThread, [
+                ThreadEvent.Delete,
+                ThreadEvent.Update,
+                ThreadEvent.NewReply,
+                RoomEvent.Timeline,
+                RoomEvent.TimelineReset,
+            ]);
+            provisionalThread.dispose();
+            for (const timelineSet of this.threadsTimelineSets) {
+                timelineSet.removeEvent(oldTargetId);
+            }
+        }
+
+        const canonicalThread =
+            this.getThread(targetEventId) ?? this.createThread(targetEventId, targetEvent, [], false);
+        for (const threadEvent of threadEvents) {
+            if (
+                !canonicalThread.initialEventsFetched &&
+                !canonicalThread.replayEvents?.includes(threadEvent)
+            ) {
+                canonicalThread.replayEvents?.push(threadEvent);
+            }
+            if (!canonicalThread.findEventById(threadEvent.getId()!)) {
+                canonicalThread.addEvent(threadEvent, false);
+            }
+        }
+    }
+
     /**
      * Applies an event as a redaction of another event, regardless of whether the redacting
      * event is actually a redaction.
diff --git a/node_modules/matrix-js-sdk/src/models/thread.ts b/node_modules/matrix-js-sdk/src/models/thread.ts
index 871875d..4d3fecd 100644
--- a/node_modules/matrix-js-sdk/src/models/thread.ts
+++ b/node_modules/matrix-js-sdk/src/models/thread.ts
@@ -185,6 +185,20 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
         this.setEventMetadata(this.rootEvent);
     }

+    /**
+     * Detach this thread from its room and timeline sources.
+     *
+     * @internal
+     */
+    public dispose(): void {
+        this.reEmitter.stopReEmitting(this.timelineSet, [RoomEvent.Timeline, RoomEvent.TimelineReset]);
+        this.room.off(MatrixEventEvent.BeforeRedaction, this.onBeforeRedaction);
+        this.room.off(RoomEvent.Redaction, this.onRedaction);
+        this.room.off(RoomEvent.LocalEchoUpdated, this.onLocalEcho);
+        this.room.off(RoomEvent.TimelineReset, this.onTimelineReset);
+        this.timelineSet.off(RoomEvent.Timeline, this.onTimelineEvent);
+    }
+
     private onTimelineReset = async (): Promise<void> => {
         // We hit a gappy sync, ask the server for an update
         await this.processRootEventPromise;
@@ -600,6 +614,11 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
     private async updateThreadMetadata(): Promise<void> {
         this.updatePendingReplyCount();

+        if (this.id.startsWith(`~${this.roomId}:`)) {
+            this.emit(ThreadEvent.Update, this);
+            return;
+        }
+
         if (!this.processRootEventPromise) {
             // We only want to do this once otherwise we end up rolling back to the last unsigned summary we have for the thread
             this.processRootEventPromise = this.updateThreadFromRootEvent();
