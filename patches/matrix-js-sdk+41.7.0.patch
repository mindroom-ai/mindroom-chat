diff --git a/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts b/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts
index ba649eb..f8aa444 100644
--- a/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts
+++ b/node_modules/matrix-js-sdk/lib/models/relations-container.d.ts
@@ -32,13 +32,13 @@ export declare class RelationsContainer {
      *
      * @param event - The event to check as relation target.
      */
-    aggregateParentEvent(event: MatrixEvent): void;
+    aggregateParentEvent(event: MatrixEvent): Promise<void>;
     /**
      * Add relation events to the relevant relation collection.
      *
      * @param event - The new child event to be aggregated.
      * @param timelineSet - The event timeline set within which to search for the related event if any.
      */
-    aggregateChildEvent(event: MatrixEvent, timelineSet?: EventTimelineSet): void;
+    aggregateChildEvent(event: MatrixEvent, timelineSet?: EventTimelineSet, targetEvent?: MatrixEvent): Promise<Relations | undefined>;
 }
 //# sourceMappingURL=relations-container.d.ts.map
\ No newline at end of file
diff --git a/node_modules/matrix-js-sdk/lib/models/relations-container.js b/node_modules/matrix-js-sdk/lib/models/relations-container.js
index 8ccc053..fb40514 100644
--- a/node_modules/matrix-js-sdk/lib/models/relations-container.js
+++ b/node_modules/matrix-js-sdk/lib/models/relations-container.js
@@ -65,12 +65,14 @@ export class RelationsContainer {
    */
   aggregateParentEvent(event) {
     var relationsForEvent = this.relations.get(event.getId());
-    if (!relationsForEvent) return;
+    if (!relationsForEvent) return Promise.resolve();
+    var targetUpdates = [];
     for (var relationsWithRelType of relationsForEvent.values()) {
       for (var relationsWithEventType of relationsWithRelType.values()) {
-        relationsWithEventType.setTargetEvent(event);
+        targetUpdates.push(relationsWithEventType.setTargetEvent(event));
       }
     }
+    return Promise.all(targetUpdates).then(() => undefined);
   }

   /**
@@ -79,26 +81,29 @@ export class RelationsContainer {
    * @param event - The new child event to be aggregated.
    * @param timelineSet - The event timeline set within which to search for the related event if any.
    */
-  aggregateChildEvent(event, timelineSet) {
+  aggregateChildEvent(event, timelineSet, targetEvent) {
     if (event.isRedacted() || event.status === EventStatus.CANCELLED) {
-      return;
+      return Promise.resolve(undefined);
     }
     var relation = event.getRelation();
-    if (!relation) return;
-    var onEventDecrypted = () => {
-      if (event.isDecryptionFailure()) {
-        // This could for example happen if the encryption keys are not yet available.
-        // The event may still be decrypted later. Register the listener again.
-        event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
-        return;
-      }
-      this.aggregateChildEvent(event, timelineSet);
-    };
+    if (!relation) return Promise.resolve(undefined);

-    // If the event is currently encrypted, wait until it has been decrypted.
+    // Wait for the clear event type before creating its relation collection.
+    // Reuse the timeline resolver after decryption so roomless containers can
+    // find the target and room-backed containers can select its freshest instance.
     if (event.isBeingDecrypted() || event.shouldAttemptDecryption()) {
-      event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
-      return;
+      return new Promise((resolve, reject) => {
+        var onEventDecrypted = () => {
+          if (event.isDecryptionFailure()) {
+            // This could for example happen if the encryption keys are not yet available.
+            // The event may still be decrypted later. Register the listener again.
+            event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
+            return;
+          }
+          void this.aggregateChildEvent(event, timelineSet, targetEvent).then(resolve, reject);
+        };
+        event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
+      });
     }
     var relatesToEventId = relation.event_id,
       relationType = relation.rel_type;
@@ -114,17 +119,23 @@ export class RelationsContainer {
       relationsForEvent.set(relationType, relationsWithRelType);
     }
     var relationsWithEventType = relationsWithRelType.get(eventType);
+    var targetEventUpdate = Promise.resolve();
     if (!relationsWithEventType) {
       var _this$room, _ref, _timelineSet$findEven;
       relationsWithEventType = new Relations(relationType, eventType, this.client);
       relationsWithRelType.set(eventType, relationsWithEventType);
       var _room = (_this$room = this.room) !== null && _this$room !== void 0 ? _this$room : timelineSet === null || timelineSet === void 0 ? void 0 : timelineSet.room;
-      var relatesToEvent = (_ref = (_timelineSet$findEven = timelineSet === null || timelineSet === void 0 ? void 0 : timelineSet.findEventById(relatesToEventId)) !== null && _timelineSet$findEven !== void 0 ? _timelineSet$findEven : _room === null || _room === void 0 ? void 0 : _room.findEventById(relatesToEventId)) !== null && _ref !== void 0 ? _ref : _room === null || _room === void 0 ? void 0 : _room.getPendingEvent(relatesToEventId);
+      var relatesToEvent = (_ref = (_timelineSet$findEven = timelineSet === null || timelineSet === void 0 ? void 0 : timelineSet.findEventById(relatesToEventId)) !== null && _timelineSet$findEven !== void 0 ? _timelineSet$findEven : _room === null || _room === void 0 ? void 0 : _room.findEventById(relatesToEventId)) !== null && _ref !== void 0 ? _ref : (_ref = _room === null || _room === void 0 ? void 0 : _room.getPendingEvent(relatesToEventId)) !== null && _ref !== void 0 ? _ref : targetEvent;
       if (relatesToEvent) {
-        relationsWithEventType.setTargetEvent(relatesToEvent);
+        targetEventUpdate = relationsWithEventType.setTargetEvent(relatesToEvent);
       }
     }
-    relationsWithEventType.addEvent(event);
+
+    // addEvent mutates the insertion-ordered relation set before its first await.
+    // Start it immediately so fire-and-forget callers and equal-timestamp edits
+    // retain arrival order while target rebinding completes.
+    var addEventUpdate = relationsWithEventType.addEvent(event);
+    return Promise.all([targetEventUpdate, addEventUpdate]).then(() => relationsWithEventType);
   }
 }
 //# sourceMappingURL=relations-container.js.map
diff --git a/node_modules/matrix-js-sdk/lib/models/relations.js b/node_modules/matrix-js-sdk/lib/models/relations.js
index 6be66bb..e3313f3 100644
--- a/node_modules/matrix-js-sdk/lib/models/relations.js
+++ b/node_modules/matrix-js-sdk/lib/models/relations.js
@@ -60,6 +60,7 @@ export class Relations extends TypedEventEmitter {
     _defineProperty(this, "annotationsBySender", {});
     _defineProperty(this, "sortedAnnotationsByKey", []);
     _defineProperty(this, "targetEvent", null);
+    _defineProperty(this, "targetEventUpdatePromise", Promise.resolve());
     _defineProperty(this, "creationEmitted", false);
     _defineProperty(this, "replacementUpdateId", 0);
     _defineProperty(this, "client", void 0);
@@ -320,13 +321,16 @@ export class Relations extends TypedEventEmitter {
   setTargetEvent(event) {
     var _this5 = this;
     return _asyncToGenerator(function* () {
-      if (_this5.targetEvent) {
-        return;
+      if (_this5.targetEvent === event) {
+        return _this5.targetEventUpdatePromise;
       }
       _this5.targetEvent = event;
       if (_this5.relationType === RelationType.Replace) {
-        yield _this5.updateTargetEventReplacement();
+        _this5.targetEventUpdatePromise = _this5.updateTargetEventReplacement();
+      } else {
+        _this5.targetEventUpdatePromise = Promise.resolve();
       }
+      yield _this5.targetEventUpdatePromise;
       _this5.maybeEmitCreated();
     })();
   }
diff --git a/node_modules/matrix-js-sdk/lib/models/thread.js b/node_modules/matrix-js-sdk/lib/models/thread.js
index b02b81f..10f8d15 100644
--- a/node_modules/matrix-js-sdk/lib/models/thread.js
+++ b/node_modules/matrix-js-sdk/lib/models/thread.js
@@ -114,6 +114,10 @@ export class Thread extends ReadReceipt {
      */
     _defineProperty(this, "initialEventsFetched", !Thread.hasServerSideSupport);
     _defineProperty(this, "initalEventFetchProm", void 0);
+    _defineProperty(this, "preInitEventTargets", new Map());
+    _defineProperty(this, "preInitObserverState", {
+      active: true
+    });
     /**
      * An array of events to add to the timeline once the thread has been initialised
      * with server suppport.
@@ -204,6 +208,9 @@ export class Thread extends ReadReceipt {
     }, this.client, this);
     this.reEmitter = new TypedReEmitter(this);
     this.reEmitter.reEmit(this.timelineSet, [RoomEvent.Timeline, RoomEvent.TimelineReset]);
+    this.once(ThreadEvent.Delete, () => {
+      this.preInitObserverState.active = false;
+    });
     this.room.on(MatrixEventEvent.BeforeRedaction, this.onBeforeRedaction);
     this.room.on(RoomEvent.Redaction, this.onRedaction);
     this.room.on(RoomEvent.LocalEchoUpdated, this.onLocalEcho);
@@ -321,6 +328,10 @@ export class Thread extends ReadReceipt {
     // Modify this event to point at our room's state, and mark its thread
     // as this.
     this.setEventMetadata(event);
+    var eventId = event.getId();
+    if (!this.initialEventsFetched && eventId && !event.isRelation(RelationType.Annotation) && !event.isRelation(RelationType.Replace)) {
+      this.preInitEventTargets.set(eventId, event);
+    }

     // Decide whether this event is going to be added at the end of the timeline.
     var lastReply = this.lastReply();
@@ -387,12 +398,27 @@ export class Thread extends ReadReceipt {
        */
       (_this$replayEvents = this.replayEvents) === null || _this$replayEvents === void 0 || _this$replayEvents.push(event);

-      // For annotations (reactions), aggregate immediately (pre-init) to keep
-      // reaction counts/summary visible while the thread is still initialising.
+      // For annotations (reactions) and replacements (edits), aggregate immediately
+      // to keep reaction counts and edited content visible while the thread is initialising.
       // Only aggregate as child: parent aggregation is unnecessary here.
-      if (event.isRelation(RelationType.Annotation)) {
-        var _this$timelineSet$rel;
-        (_this$timelineSet$rel = this.timelineSet.relations) === null || _this$timelineSet$rel === void 0 || _this$timelineSet$rel.aggregateChildEvent(event, this.timelineSet);
+      if (event.isRelation(RelationType.Annotation) || event.isRelation(RelationType.Replace)) {
+        var relation = event.getRelation();
+        var targetEventId = relation === null || relation === void 0 ? void 0 : relation.event_id;
+        var targetEvent = event.isRelation(RelationType.Replace) && targetEventId ? this.findPreInitTargetEvent(targetEventId) : undefined;
+        var aggregation = this.timelineSet.relations.aggregateChildEvent(event, this.timelineSet, targetEvent);
+        if (event.isRelation(RelationType.Replace) && targetEventId) {
+          var weakThread = new WeakRef(this);
+          var observerState = this.preInitObserverState;
+          void aggregation.then(() => {
+            var _effectiveTarget$rep;
+            var thread = weakThread.deref();
+            if (!thread || !observerState.active) return;
+            var effectiveTarget = thread.findPreInitTargetEvent(targetEventId);
+            if (((_effectiveTarget$rep = effectiveTarget === null || effectiveTarget === void 0 ? void 0 : effectiveTarget.replacingEvent()) === null || _effectiveTarget$rep === void 0 ? void 0 : _effectiveTarget$rep.getId()) === event.getId()) {
+              thread.emit(ThreadEvent.Update, thread);
+            }
+          }, error => logger.error("Failed to aggregate pre-initialization thread relation: ", error));
+        }
       }
     } else {
       var _this$client$canSuppo2;
@@ -422,6 +448,10 @@ export class Thread extends ReadReceipt {
       // Aggregation is handled by EventTimelineSet when inserting/adding.
     }
   }
+  findPreInitTargetEvent(eventId) {
+    var _this$timelineSet$fin, _this$room$findEventB;
+    return (_this$timelineSet$fin = this.timelineSet.findEventById(eventId)) !== null && _this$timelineSet$fin !== void 0 ? _this$timelineSet$fin : (_this$room$findEventB = this.room.findEventById(eventId)) !== null && _this$room$findEventB !== void 0 ? _this$room$findEventB : this.preInitEventTargets.get(eventId);
+  }
   processEvent(event) {
     var _this3 = this;
     return _asyncToGenerator(function* () {
@@ -556,7 +586,14 @@ export class Thread extends ReadReceipt {
       yield _this7.processRootEventPromise;
       if (!_this7.initialEventsFetched) {
         if (_this7.initalEventFetchProm) {
-          yield _this7.initalEventFetchProm;
+          try {
+            yield _this7.initalEventFetchProm;
+          } catch (_unused) {
+            // A concurrent metadata update owns the request and reports the failure.
+            // Keep this waiter from surfacing the same rejection as unhandled.
+            _this7.initalEventFetchProm = undefined;
+            _this7.initialEventsFetched = false;
+          }
         } else {
           // fetch initial events to allow proper pagination
           try {
@@ -585,16 +622,19 @@ export class Thread extends ReadReceipt {
             // must remain false while the async fetch happens, so we don't try to add events before
             // the pagination has finished. The important thing is that we're not await-ing anything
             // else between setting this and adding events, so no races.
+            yield Promise.all(_this7.liveTimeline.getEvents().map(event => _this7.timelineSet.relations.aggregateParentEvent(event)));
             _this7.initialEventsFetched = true;
             for (var event of _this7.replayEvents) {
               _this7.addEvent(event, false);
             }
             _this7.replayEvents = null;
+            _this7.preInitEventTargets.clear();
             // just to make sure that, if we've created a timeline window for this thread before the thread itself
             // existed (e.g. when creating a new thread), we'll make sure the panel is force refreshed correctly.
             _this7.emit(RoomEvent.TimelineReset, _this7.room, _this7.timelineSet, true);
           } catch (e) {
             logger.error("Failed to load start of newly created thread: ", e);
+            _this7.initalEventFetchProm = undefined;
             _this7.initialEventsFetched = false;
           }
         }
diff --git a/node_modules/matrix-js-sdk/src/models/relations-container.ts b/node_modules/matrix-js-sdk/src/models/relations-container.ts
index 8394bc6..132010b 100644
--- a/node_modules/matrix-js-sdk/src/models/relations-container.ts
+++ b/node_modules/matrix-js-sdk/src/models/relations-container.ts
@@ -72,15 +72,17 @@ export class RelationsContainer {
      *
      * @param event - The event to check as relation target.
      */
-    public aggregateParentEvent(event: MatrixEvent): void {
+    public aggregateParentEvent(event: MatrixEvent): Promise<void> {
         const relationsForEvent = this.relations.get(event.getId()!);
-        if (!relationsForEvent) return;
+        if (!relationsForEvent) return Promise.resolve();

+        const targetUpdates: Promise<void>[] = [];
         for (const relationsWithRelType of relationsForEvent.values()) {
             for (const relationsWithEventType of relationsWithRelType.values()) {
-                relationsWithEventType.setTargetEvent(event);
+                targetUpdates.push(relationsWithEventType.setTargetEvent(event));
             }
         }
+        return Promise.all(targetUpdates).then(() => undefined);
     }

     /**
@@ -89,29 +91,35 @@ export class RelationsContainer {
      * @param event - The new child event to be aggregated.
      * @param timelineSet - The event timeline set within which to search for the related event if any.
      */
-    public aggregateChildEvent(event: MatrixEvent, timelineSet?: EventTimelineSet): void {
+    public aggregateChildEvent(
+        event: MatrixEvent,
+        timelineSet?: EventTimelineSet,
+        targetEvent?: MatrixEvent,
+    ): Promise<Relations | undefined> {
         if (event.isRedacted() || event.status === EventStatus.CANCELLED) {
-            return;
+            return Promise.resolve(undefined);
         }

         const relation = event.getRelation();
-        if (!relation) return;
+        if (!relation) return Promise.resolve(undefined);

-        const onEventDecrypted = (): void => {
-            if (event.isDecryptionFailure()) {
-                // This could for example happen if the encryption keys are not yet available.
-                // The event may still be decrypted later. Register the listener again.
-                event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
-                return;
-            }
-
-            this.aggregateChildEvent(event, timelineSet);
-        };
-
-        // If the event is currently encrypted, wait until it has been decrypted.
+        // Wait for the clear event type before creating its relation collection.
+        // Reuse the timeline resolver after decryption so roomless containers can
+        // find the target and room-backed containers can select its freshest instance.
         if (event.isBeingDecrypted() || event.shouldAttemptDecryption()) {
-            event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
-            return;
+            return new Promise<Relations | undefined>((resolve, reject) => {
+                const onEventDecrypted = (): void => {
+                    if (event.isDecryptionFailure()) {
+                        // This could for example happen if the encryption keys are not yet available.
+                        // The event may still be decrypted later. Register the listener again.
+                        event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
+                        return;
+                    }
+
+                    void this.aggregateChildEvent(event, timelineSet, targetEvent).then(resolve, reject);
+                };
+                event.once(MatrixEventEvent.Decrypted, onEventDecrypted);
+            });
         }

         const { event_id: relatesToEventId, rel_type: relationType } = relation;
@@ -130,6 +138,7 @@ export class RelationsContainer {
         }

         let relationsWithEventType = relationsWithRelType.get(eventType);
+        let targetEventUpdate = Promise.resolve();
         if (!relationsWithEventType) {
             relationsWithEventType = new Relations(relationType!, eventType, this.client);
             relationsWithRelType.set(eventType, relationsWithEventType);
@@ -138,12 +147,17 @@ export class RelationsContainer {
             const relatesToEvent =
                 timelineSet?.findEventById(relatesToEventId!) ??
                 room?.findEventById(relatesToEventId!) ??
-                room?.getPendingEvent(relatesToEventId!);
+                room?.getPendingEvent(relatesToEventId!) ??
+                targetEvent;
             if (relatesToEvent) {
-                relationsWithEventType.setTargetEvent(relatesToEvent);
+                targetEventUpdate = relationsWithEventType.setTargetEvent(relatesToEvent);
             }
         }

-        relationsWithEventType.addEvent(event);
+        // addEvent mutates the insertion-ordered relation set before its first await.
+        // Start it immediately so fire-and-forget callers and equal-timestamp edits
+        // retain arrival order while target rebinding completes.
+        const addEventUpdate = relationsWithEventType.addEvent(event);
+        return Promise.all([targetEventUpdate, addEventUpdate]).then(() => relationsWithEventType);
     }
 }
diff --git a/node_modules/matrix-js-sdk/src/models/relations.ts b/node_modules/matrix-js-sdk/src/models/relations.ts
index 77ba3b3..ce2116b 100644
--- a/node_modules/matrix-js-sdk/src/models/relations.ts
+++ b/node_modules/matrix-js-sdk/src/models/relations.ts
@@ -52,6 +52,7 @@ export class Relations extends TypedEventEmitter<RelationsEvent, EventHandlerMap
     private annotationsBySender: Record<string, Set<MatrixEvent>> = {};
     private sortedAnnotationsByKey: [string, Set<MatrixEvent>][] = [];
     private targetEvent: MatrixEvent | null = null;
+    private targetEventUpdatePromise: Promise<void> = Promise.resolve();
     private creationEmitted = false;
     private replacementUpdateId = 0;
     private readonly client: MatrixClient;
@@ -336,14 +337,17 @@ export class Relations extends TypedEventEmitter<RelationsEvent, EventHandlerMap
      * @param targetEvent - the event the relations are related to.
      */
     public async setTargetEvent(event: MatrixEvent): Promise<void> {
-        if (this.targetEvent) {
-            return;
+        if (this.targetEvent === event) {
+            return this.targetEventUpdatePromise;
         }
         this.targetEvent = event;

         if (this.relationType === RelationType.Replace) {
-            await this.updateTargetEventReplacement();
+            this.targetEventUpdatePromise = this.updateTargetEventReplacement();
+        } else {
+            this.targetEventUpdatePromise = Promise.resolve();
         }
+        await this.targetEventUpdatePromise;

         this.maybeEmitCreated();
     }
diff --git a/node_modules/matrix-js-sdk/src/models/thread.ts b/node_modules/matrix-js-sdk/src/models/thread.ts
index 871875d..9f0f83c 100644
--- a/node_modules/matrix-js-sdk/src/models/thread.ts
+++ b/node_modules/matrix-js-sdk/src/models/thread.ts
@@ -132,6 +132,8 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
      */
     public initialEventsFetched = !Thread.hasServerSideSupport;
     private initalEventFetchProm: Promise<boolean> | undefined;
+    private preInitEventTargets = new Map<string, MatrixEvent>();
+    private preInitObserverState = { active: true };

     /**
      * An array of events to add to the timeline once the thread has been initialised
@@ -171,6 +173,10 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM

         this.reEmitter.reEmit(this.timelineSet, [RoomEvent.Timeline, RoomEvent.TimelineReset]);

+        this.once(ThreadEvent.Delete, () => {
+            this.preInitObserverState.active = false;
+        });
+
         this.room.on(MatrixEventEvent.BeforeRedaction, this.onBeforeRedaction);
         this.room.on(RoomEvent.Redaction, this.onRedaction);
         this.room.on(RoomEvent.LocalEchoUpdated, this.onLocalEcho);
@@ -364,6 +370,15 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
         // Modify this event to point at our room's state, and mark its thread
         // as this.
         this.setEventMetadata(event);
+        const eventId = event.getId();
+        if (
+            !this.initialEventsFetched &&
+            eventId &&
+            !event.isRelation(RelationType.Annotation) &&
+            !event.isRelation(RelationType.Replace)
+        ) {
+            this.preInitEventTargets.set(eventId, event);
+        }

         // Decide whether this event is going to be added at the end of the timeline.
         const lastReply = this.lastReply();
@@ -438,11 +453,36 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
              */
             this.replayEvents?.push(event);

-            // For annotations (reactions), aggregate immediately (pre-init) to keep
-            // reaction counts/summary visible while the thread is still initialising.
+            // For annotations (reactions) and replacements (edits), aggregate immediately
+            // to keep reaction counts and edited content visible while the thread is initialising.
             // Only aggregate as child: parent aggregation is unnecessary here.
-            if (event.isRelation(RelationType.Annotation)) {
-                this.timelineSet.relations?.aggregateChildEvent(event, this.timelineSet);
+            if (event.isRelation(RelationType.Annotation) || event.isRelation(RelationType.Replace)) {
+                const relation = event.getRelation();
+                const targetEventId = relation?.event_id;
+                const targetEvent =
+                    event.isRelation(RelationType.Replace) && targetEventId
+                        ? this.findPreInitTargetEvent(targetEventId)
+                        : undefined;
+                const aggregation = this.timelineSet.relations.aggregateChildEvent(
+                    event,
+                    this.timelineSet,
+                    targetEvent,
+                );
+                if (event.isRelation(RelationType.Replace) && targetEventId) {
+                    const weakThread = new WeakRef(this);
+                    const observerState = this.preInitObserverState;
+                    void aggregation.then(
+                        () => {
+                            const thread = weakThread.deref();
+                            if (!thread || !observerState.active) return;
+                            const effectiveTarget = thread.findPreInitTargetEvent(targetEventId);
+                            if (effectiveTarget?.replacingEvent()?.getId() === event.getId()) {
+                                thread.emit(ThreadEvent.Update, thread);
+                            }
+                        },
+                        (error) => logger.error("Failed to aggregate pre-initialization thread relation: ", error),
+                    );
+                }
             }
         } else {
             // Case 2: this is happening later, and we have a timeline. In
@@ -474,6 +514,14 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
         }
     }

+    private findPreInitTargetEvent(eventId: string): MatrixEvent | undefined {
+        return (
+            this.timelineSet.findEventById(eventId) ??
+            this.room.findEventById(eventId) ??
+            this.preInitEventTargets.get(eventId)
+        );
+    }
+
     public async processEvent(event: MatrixEvent | null | undefined): Promise<void> {
         if (event) {
             this.setEventMetadata(event);
@@ -608,7 +656,14 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM

         if (!this.initialEventsFetched) {
             if (this.initalEventFetchProm) {
-                await this.initalEventFetchProm;
+                try {
+                    await this.initalEventFetchProm;
+                } catch {
+                    // A concurrent metadata update owns the request and reports the failure.
+                    // Keep this waiter from surfacing the same rejection as unhandled.
+                    this.initalEventFetchProm = undefined;
+                    this.initialEventsFetched = false;
+                }
             } else {
                 // fetch initial events to allow proper pagination
                 try {
@@ -637,16 +692,23 @@ export class Thread extends ReadReceipt<ThreadEmittedEvents, ThreadEventHandlerM
                     // must remain false while the async fetch happens, so we don't try to add events before
                     // the pagination has finished. The important thing is that we're not await-ing anything
                     // else between setting this and adding events, so no races.
+                    await Promise.all(
+                        this.liveTimeline
+                            .getEvents()
+                            .map((event) => this.timelineSet.relations.aggregateParentEvent(event)),
+                    );
                     this.initialEventsFetched = true;
                     for (const event of this.replayEvents!) {
                         this.addEvent(event, false);
                     }
                     this.replayEvents = null;
+                    this.preInitEventTargets.clear();
                     // just to make sure that, if we've created a timeline window for this thread before the thread itself
                     // existed (e.g. when creating a new thread), we'll make sure the panel is force refreshed correctly.
                     this.emit(RoomEvent.TimelineReset, this.room, this.timelineSet, true);
                 } catch (e) {
                     logger.error("Failed to load start of newly created thread: ", e);
+                    this.initalEventFetchProm = undefined;
                     this.initialEventsFetched = false;
                 }
             }
