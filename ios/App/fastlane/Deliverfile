app_identifier "chat.mindroom.app"

# Uploading metadata/screenshots must never accidentally submit or release.
submit_for_review false
automatic_release false

# The app has no in-app purchases; skipping avoids requiring extra API-key scope.
precheck_include_in_app_purchases false
